name: rogue_shooter
description: A new Flutter project.

publish_to: none # this has local path dependencies

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: '1.0.0-rc10'
  joystick_plugin:
    path: ../../joystick_plugin/

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/player.png
    - assets/images/enemy.png
    - assets/images/bullet.png
    - assets/images/explosion.png
    - assets/images/stars.png
